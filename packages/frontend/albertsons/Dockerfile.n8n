# base image
FROM node:22-alpine

# workdir
WORKDIR /app

# copy the files
COPY . .

# install pnpm and run build
RUN npm install -g pnpm \
  && pnpm install --prod \
  && pnpm build

# serve build
EXPOSE 5678
CMD ["pnpm", "start"]
